import { requireApiUser } from "@/lib/auth/session";
import { mediaBucket, publicMediaUrl, PREFIX, validKey } from "@/lib/media/r2";
export const runtime = "edge";
export async function GET(request: Request) { const guard = await requireApiUser(request); if (!guard.ok) return guard.response; const bucket = await mediaBucket(); if (!bucket) return Response.json({ error: "R2 binding `MEDIA` is not configured." }, { status: 503 }); const result = await bucket.list({ prefix: PREFIX }); return Response.json({ data: await Promise.all(result.objects.map(async (item) => ({ key: item.key, size: item.size, contentType: item.httpMetadata?.contentType ?? "application/octet-stream", url: await publicMediaUrl(item.key) ?? new URL(`/media/${item.key}`, request.url).toString() }))) }); }
export async function DELETE(request: Request) { const guard = await requireApiUser(request); if (!guard.ok) return guard.response; const key = new URL(request.url).searchParams.get("key") ?? ""; if (!validKey(key)) return Response.json({ error: "Invalid media key." }, { status: 400 }); const bucket = await mediaBucket(); if (!bucket) return Response.json({ error: "R2 binding `MEDIA` is not configured." }, { status: 503 }); await bucket.delete(key); return Response.json({ ok: true }); }
